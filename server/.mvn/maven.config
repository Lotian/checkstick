# 依赖仓库使用各机器自己的默认位置（~/.m2/repository）。
#
# 这里原先写死了 Windows 路径 -Dmaven.repo.local=E:/Develop/maven_repos，
# 会导致 Linux 服务器把依赖下载到 ./E:/Develop/maven_repos 这种畸形目录里。
# 需要指定仓库位置时，在命令行显式传入，例如：
#   ./mvnw -Dmaven.repo.local=/opt/xiqian/.m2/repository clean package
